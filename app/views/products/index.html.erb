<div class="products-index-title">
  <h1><%= t ".title" %></h1>
  <%= link_to t(".new"), new_product_path if authenticated? %>
</div>

<div id="products">
  <% @products.each do |product| %>
    <div>
      <%= link_to product.name, product %>
    </div>
  <% end %>
</div>
