<% cache @product do %>
    <h1><%= @product.name %></h1>
    <%= @product.description %>
    <%= image_tag @product.featured_image if @product.featured_image.attached? %>
<% end %>

<% if authenticated? %>
    <%= link_to t("show.edit"), edit_product_path(@product) %>
    <%= button_to t("show.delete"), @product, method: :delete, data: { turbo_confirm: "Are you sure?" } %>
<% end %>

<%= link_to t("back"), root_path %>